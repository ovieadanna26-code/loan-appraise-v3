const supabase=createClient(window.SUPABASE_URL,window.SUPABASE_PUBLISHABLE_KEY);const app=document.getElementById('app');let session=null;async function init(){const s=await supabase.auth.getSession();session=s.data.session;if(!session)return login();await dashboard()}function login(){app.innerHTML='<main class="auth"><section class="card"><h1>Loan Appraise V3</h1><p>Secure loan appraisal and approval.</p><input id="email" type="email" placeholder="Email"><input id="password" type="password" placeholder="Password"><button class="primary" id="signin">Sign In</button><p id="msg"></p></section></main>';document.getElementById('signin').onclick=async()=>{const msg=document.getElementById('msg');msg.textContent='Signing in...';const {error}=await supabase.auth.signInWithPassword({email:email.value,password:password.value});if(error)msg.textContent=error.message;else location.reload()}}async function dashboard(){const {data}=await supabase.from('loan_applications').select('id,reference,status,amount,created_at,customers(full_name)').order('created_at',{ascending:false});app.innerHTML='<div class="shell"><aside class="side"><h2>Loan Appraise</h2><button id="new">+ New Application</button><button id="logout">Sign Out</button></aside><main class="main"><header><div><h1>Loan Officer Dashboard</h1><p>Applications</p></div><button class="primary" id="new2">+ New Application</button></header><section class="card"><table><thead><tr><th>Reference</th><th>Customer</th><th>Amount</th><th>Status</th></tr></thead><tbody>'+(data||[]).map(x=>`<tr><td>${x.reference}</td><td>${x.customers?.full_name||''}</td><td>₦${Number(x.amount||0).toLocaleString()}</td><td>${x.status}</td></tr>`).join('')+'</tbody></table></section></main></div>';document.getElementById('new').onclick=document.getElementById('new2').onclick=()=>application();document.getElementById('logout').onclick=async()=>{await supabase.auth.signOut();location.reload()}}function application(){let state={};const steps=['Customer','Loan Request','Business & Sales','Financials','Products','Balance Sheet','Collateral','Review'];let i=0;const render=()=>{app.innerHTML=`<div class="shell"><aside class="side"><h2>Loan Appraise</h2><button id="dash">Dashboard</button><button id="logout">Sign Out</button></aside><main class="main"><header><div><h1>New Loan Application</h1><p>Step ${i+1} of 8</p></div><button class="secondary" id="save">Save Draft</button></header><nav class="steps">${steps.map((s,n)=>`<span class="${n===i?'active':''}">${n+1}. ${s}</span>`).join('')}</nav><section class="card"><h2>${steps[i]}</h2><p>This V3 section is being built on the clean database foundation.</p><div class="actions"><button class="secondary" id="back" ${i===0?'disabled':''}>Back</button><button class="primary" id="next">Save & Continue</button></div></section></main></div>`;document.getElementById('dash').onclick=dashboard;document.getElementById('logout').onclick=async()=>{await supabase.auth.signOut();location.reload()};document.getElementById('back').onclick=()=>{if(i>0){i--;render()}};document.getElementById('next').onclick=()=>{if(i<7){i++;render()}};document.getElementById('save').onclick=()=>alert('Draft save will be connected section-by-section next.')};render()}init();